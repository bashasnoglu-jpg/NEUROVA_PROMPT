#!/bin/sh
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "server/validate_visual_library_lock.ps1"
status=$?
if [ $status -ne 0 ]; then
  echo "VISUAL_LIBRARY_LOCK validator failed."
  exit $status
fi
exit 0